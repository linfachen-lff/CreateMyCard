interface:
  display_name: "Harmony Form 卡片生成（DataModel 优先）"
  short_description: "优先使用完整表达式读取 DataModel，生成协议合规的 HarmonyOS A2UI Form 服务卡片。"
  default_prompt: "使用 $harmony-card-generation-datamodel-first 根据一句话生成 HarmonyOS A2UI Form 服务卡片，优先使用完整表达式读取 DataModel，遵循协议、布局和视觉硬门槛，最后只输出 genui 和 cardspec 代码块。"
